{
  "name": "@applications/project",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "dependencies": {
    "@config/main": "*",
    "@components/Button": "*"
  },
  "scripts": {
    "start": "npx webpack-dev-server --color",
    "build": "NODE_ENV=production npx webpack --progress --hide-modules",
    "clear": "rm -rf dest node_modules",
    "test": "npx jest --coverage",
    "test:watch": "npx jest --watch --coverage",
    "locale": "NODE_ENV=locales npx babel ./src >/dev/null && node ./locale.config.js"
  }
}
